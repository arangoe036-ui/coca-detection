# Mirror of pyproject.toml dependencies for pip/uv users who prefer requirements.txt.
# Install:  uv pip install -r requirements.txt   (or)   pip install -r requirements.txt
# torch may need a platform-specific index; see https://pytorch.org/get-started/locally/

# ML
torch>=2.2
segmentation-models-pytorch>=0.3.3
torchgeo>=0.6

# Geodata / imagery access (Microsoft Planetary Computer STAC — no GEE)
pystac-client>=0.8
planetary-computer>=1.0
odc-stac>=0.3
rioxarray>=0.15
xarray>=2024.1
dask>=2024.1
rasterio>=1.3
geopandas>=0.14
shapely>=2.0
pyproj>=3.6

# Utils
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
albumentations>=1.4
pyyaml>=6.0
tqdm>=4.66

# Viz / maps
matplotlib>=3.8
leafmap>=0.30
folium>=0.16
